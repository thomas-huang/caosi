# Provider File is providers.jsonc

The Provider map lives at `<config-dir>/providers.jsonc` (default `~/.caosi/providers.jsonc`). JSONC keeps the original JSON shape and adds comments and trailing commas. YAML was rejected so the file stays an object keyed by Provider Name, not an indentation-sensitive document.
