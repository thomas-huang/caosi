# Loopback is the security model

caosi does not authenticate clients. Anyone who can reach the loopback port can use every Provider. A second local token would make every client reconfigure just to talk to a machine-local converter; the bind address is the access control.
