# First run writes a template

If the config file is missing, caosi creates `~/.caosi/` and a commented sample, then exits. It does not start empty and 404 every Provider, and it does not refuse to touch the disk. The process only serves after the user has filled in real credentials and started it again.
